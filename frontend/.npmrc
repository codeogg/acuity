# Hoist peer deps so a single React instance is shared across the design-kit
# tarball, the theme overlay, and every app (prevents dual-React).
node-linker=hoisted
shamefully-hoist=true
auto-install-peers=true
